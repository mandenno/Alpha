<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_gravity="center"
    android:padding="40dp"
    android:layout_height="fill_parent"
    android:orientation="vertical">


    <Button
        android:id="@+id/Scan"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/print_txt"
        android:layout_alignParentStart="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="70dp"
        android:background="@mipmap/shapeit"
        android:drawableLeft="@drawable/bt_scan"
        android:paddingLeft="5dp"
        android:text="Scan PRINTER"
        android:textAlignment="center"
        android:textColor="@color/colorWhite"></Button>

    <Button
        android:id="@+id/mPrint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/Scan"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="40dp"
        android:background="@mipmap/shapeit"
        android:backgroundTint="@color/colorPrimary"
        android:drawableLeft="@drawable/bt_print"
        android:paddingLeft="5dp"
        android:text="Print receipt"
        android:textColor="@color/colorWhite"></Button>

    <Button
        android:id="@+id/dis"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/mPrint"
        android:layout_alignParentStart="true"
        android:layout_marginTop="30dp"
        android:background="@mipmap/shapeit"
        android:backgroundTint="@color/proceed_disable"
        android:text="Disable"
        android:textColor="@color/colorWhite"></Button>

    <TextView
        android:id="@+id/print_txt"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="32dp"
        android:layout_marginEnd="0dp"
        android:text="VERIFICATION RECEIPT"
        android:textAlignment="center"
        android:textSize="18sp" />

    <Button
        android:id="@+id/donebtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/dis"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="100dp"
        android:background="@mipmap/shapeit"
        android:backgroundTint="@color/proceed_disable"
        android:text="DONE" />
</RelativeLayout>